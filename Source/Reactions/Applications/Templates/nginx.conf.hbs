events { worker_connections 1024; }

http {
    server {
        listen 80;

        location / {
            proxy_pass http://kernelf39633a1.internal.blackstone-c19df85b.westeurope.azurecontainerapps.io;
            proxy_http_version 1.1;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
